app_identifier "torkve.bidichan"
team_id ENV["APPLE_TEAM_ID"]
itc_team_id ENV["APPLE_TEAM_ID"]
# apple_id is only needed for the rare produce fallback that requires an
# Apple-ID session; the API-key path (see Fastfile) doesn't use it.
apple_id ENV["FASTLANE_APPLE_ID"] if ENV["FASTLANE_APPLE_ID"]
