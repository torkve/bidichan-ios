source "https://rubygems.org"

# Use a current fastlane: 2.227.x sends a `templateName` attribute the App Store
# Connect API now rejects when creating provisioning profiles. Keep it recent;
# pin to a specific version only after a green bootstrap + TestFlight run.
gem "fastlane", ">= 2.228"
gem "xcodeproj"
